{% extends 'base.html.twig' %}

{% block body %}
<div class="min-vh-100 d-flex align-items-center justify-content-center bg-light">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6 text-center">
                <!-- Icône d'erreur -->
                <div class="mb-4">
                    <i class="fas fa-exclamation-circle text-danger" style="font-size: 5rem;"></i>
                </div>

                <!-- Message principal -->
                <h1 class="display-4 mb-4 text-danger fw-bold">Page non trouvée</h1>
                
                <!-- Message d'erreur détaillé -->
                <div class="card shadow-sm">
                    <div class="card-body">
                        <p class="lead mb-4">
                            Oups ! La page que vous recherchez semble introuvable. 
                            Cela peut être dû à une erreur de frappe dans l'URL ou la page a peut-être été déplacée.
                        </p>
                        
                        <!-- Suggestions et actions -->
                        <div class="mb-4">
                            <p class="text-muted">Vous pouvez essayer de :</p>
                            <ul class="list-unstyled">
                                <li class="mb-2">
                                    <i class="fas fa-arrow-left me-2"></i>
                                    <a href="javascript:history.back()" class="text-decoration-none">Retourner à la page précédente</a>
                                </li>
                                <li class="mb-2">
                                    <i class="fas fa-home me-2"></i>
                                    <a href="{{ path('app_homepage') }}" class="text-decoration-none">Revenir à l'accueil</a>
                                </li>
                            </ul>
                        </div>
                        
                        <!-- Code d'erreur -->
                        <div class="text-muted">
                            <small>Erreur : {{status_code}}</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
{% endblock %}